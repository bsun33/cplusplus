#include <iostream>

/* comment pairs /* */  /*cannot nest. */
/*
* ''cannot nest'' is considered source code,
* as is the rest of the program
*/
int main()
{
	//always to use std::endl to flush the buffer
	std::cout << "Hello, World" << std::endl;

	return 0;
}